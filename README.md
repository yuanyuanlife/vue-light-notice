# vue-light-notice
semi opacity notify plugin for vue.js

* type: 'success' 'warn' 'failure' 'loading' 'text' ''
* message: message to show
* usage:

```js
 this.$opacityNotify({
  type: 'success',
  message: 'success!',
  always: false
 });
```
